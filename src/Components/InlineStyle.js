import React from 'react'

export default function InlineStyle() {
    const heading={
        fontSize:'80px',
        color:'yellow'
    }
    return (
        <div style={heading}>INLINE</div>
    );
}
